import { OrderByAliasesPipe } from './order-by-aliases.pipe';

describe('OrderByAliasesPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderByAliasesPipe();
    expect(pipe).toBeTruthy();
  });
});
